/// English — the words the widgets already say with no theme at all.
///
/// It is here as a file rather than only as [PlassLabels.english] so that a
/// locale is one shape everywhere: a caller building their own starts from this
/// one, and a reviewer comparing two languages is comparing two identical files.
library;

import 'package:plass_ui/src/internal/date.dart';

/// The library's own vocabulary in English.
const PlassLabels en = PlassLabels.english;
