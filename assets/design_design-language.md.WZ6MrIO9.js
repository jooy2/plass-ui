import{_ as t,o as a,c as s,a2 as o}from"./chunks/framework.M7K9TQZf.js";const g=JSON.parse('{"title":"Design language","description":"A Plass surface is a key of tinted glass resting on a clear sheet. That one sentence is the reason for every rule below. When a new component leaves you…","frontmatter":{"title":"Design language","order":1},"headers":[],"relativePath":"design/design-language.md","filePath":"en/design/design-language.md","lastUpdated":1788614679000}'),i={name:"design/design-language.md"};function n(r,e,d,h,l,c){return a(),s("div",null,[...e[0]||(e[0]=[o('<h1 id="design-language" tabindex="-1">Design language <a class="header-anchor" href="#design-language" aria-label="Permalink to &quot;Design language&quot;">​</a></h1><p>A Plass surface is <strong>a key of tinted glass resting on a clear sheet</strong>. That one sentence is the reason for every rule below. When a new component leaves you unsure, come back to it, and to the question it makes every surface answer:</p><blockquote><p><strong>Is this thing pressed, or does it hold something?</strong></p></blockquote><p>A thing that is pressed is tinted glass: a gradient that sweeps between two ends of its colour family, a drop shadow in that family, and a bloom of light that follows the pointer across it. A thing that holds something is clear glass: translucent, heavily blurred, a white hairline round it, and never dyed. There is no third answer, and a component that seems to need one is two components.</p><p>What that sentence takes, and what it leaves out:</p><ul><li><strong>Taken</strong>: a light direction, colour that bleeds into the shadow a control casts, and light that arrives with the pointer.</li><li><strong>Dropped</strong>: relief of every kind (bevels, specular highlights, the dark line under a key, bulk), and any state expressed by making a control move.</li></ul><blockquote><p><strong>This is where Plass stopped being a plastic library.</strong> An earlier draft drew a filled control as a moulded key: a three-stop gradient that lightened at one corner and darkened at the other, with a specular highlight laid over the top half to finish it. Everything about it was internally consistent and all of it read as lacquer. What replaced it is below, and it is one idea: <strong>a gradient that turns instead of one that shades.</strong></p></blockquote><hr><h2 id="_1-the-two-materials" tabindex="-1">1. The two materials <a class="header-anchor" href="#_1-the-two-materials" aria-label="Permalink to &quot;1. The two materials&quot;">​</a></h2><h3 id="tinted-glass" tabindex="-1">Tinted glass <a class="header-anchor" href="#tinted-glass" aria-label="Permalink to &quot;Tinted glass&quot;">​</a></h3><p>A <code>solid</code> surface is three layers, and there are deliberately only three.</p><table tabindex="0"><thead><tr><th>Layer</th><th>Token</th><th>Job</th></tr></thead><tbody><tr><td>Fill</td><td><code>--plass-{color}-fill</code></td><td>A two-stop gradient at 135°</td></tr><tr><td>Tint</td><td><code>--plass-{color}-tint</code></td><td>The drop shadow, in the control&#39;s own colour</td></tr><tr><td>Glow</td><td><code>.plass-glow</code></td><td>A bloom of light that follows the pointer</td></tr></tbody></table><p><strong>The fill is a gradient, and the gradient turns rather than shades.</strong> The two stops are the two ends of the colour family at <em>one lightness</em>: primary runs indigo to azure, danger vermilion to rose, success green to teal, info blue to cyan. Nothing gets lighter and nothing gets darker.</p><p>That is the whole reason there is no highlight layer. A gradient that darkens toward one corner is a moulded object catching a lamp, and an object like that needs a specular highlight to finish the illusion, which is exactly what made a filled control read as lacquer. A gradient that turns is a pane of tinted glass, and a pane needs nothing else.</p><p><strong>No highlight on a filled control, and no bevel under it.</strong> An inset white line along the top edge (<code>inset 0 1px 0 white</code>) is the single fastest way to make a coloured surface look moulded; <code>inset 0 -1px 0 black</code> under it is the second. The hairline belongs to <code>glass</code>, which has a real cut edge for light to catch. A <code>solid</code> control has one shadow and no edge at all.</p><p><strong>135° everywhere.</strong> The fill and the elevation both assume light arriving from the top-left. One of them pointing somewhere else makes the object read as two objects.</p><h3 id="glass" tabindex="-1">Glass <a class="header-anchor" href="#glass" aria-label="Permalink to &quot;Glass&quot;">​</a></h3><p>Everything that holds content is one sheet at three strengths.</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>--plass-glass         rgb(255 255 255 / 0.62)   at rest</span></span>\n<span class="line"><span>--plass-glass-hover   rgb(255 255 255 / 0.76)</span></span>\n<span class="line"><span>--plass-glass-press   rgb(255 255 255 / 0.88)</span></span></code></pre></div><p><strong>The ladder is opacity, not lightness.</strong> As a surface is engaged it holds more light, rather than turning grey.</p><p><strong>The blur is the material.</strong> <code>blur(22px) saturate(160%)</code>, deliberately a generous smear. Plass is not trying to let you read what is behind a sheet; it is trying to make the sheet look thick. Below about 14px the glass stops being glass and becomes a white box with an alpha on it.</p><p><strong>Glass is never dyed.</strong> A sheet holds other people&#39;s content, and that content arrives with its own colours: body text, links, buttons, fields. Tinting the sheet underneath puts every one of them on a background they were not chosen against. So <strong>the family stops at the hairline, the focus ring and the caret, and the glass stays clear.</strong></p><p>Controls are the opposite case and take the family into the fill itself, because a PlButton&#39;s surface <em>is</em> the thing being coloured.</p><blockquote><p><strong>One consequence.</strong> On a <code>glass</code> PlButton, <code>color</code> is the label and the hairline. On a <code>glass</code> PlTextField, <code>color</code> is the hairline, the ring and the caret and nothing else, which is why an invalid field can turn the whole family over to <code>danger</code> without repainting its surface.</p></blockquote><h3 id="the-one-inset-shadow" tabindex="-1">The one inset shadow <a class="header-anchor" href="#the-one-inset-shadow" aria-label="Permalink to &quot;The one inset shadow&quot;">​</a></h3><p><code>--plass-well</code> is the exception that proves the pair: the only shadow in the library that points <strong>inward</strong>. It is what a <code>solid</code> PlTextField is drawn as, the glass at its most opaque with light falling into it, because a gradient under a caret, a text selection and a placeholder is not legible.</p><p>So <code>solid</code> means &quot;the tinted pane&quot; on anything pressed and &quot;the deepest clear glass&quot; on anything typed into. Same word, and one rule underneath it: <code>solid</code> is the heaviest thing that variant can be while still doing its job.</p><hr><h2 id="_2-colour" tabindex="-1">2. Colour <a class="header-anchor" href="#_2-colour" aria-label="Permalink to &quot;2. Colour&quot;">​</a></h2><p>This section is the <em>why</em>. For what the tokens actually resolve to and how to override them, see <a href="./color">Colour</a>.</p><p>The base colour is <code>#3558ef</code>. Everything else comes off its palette.</p><table tabindex="0"><thead><tr><th>Role</th><th>Where it comes from</th></tr></thead><tbody><tr><td><code>primary</code></td><td>The base colour</td></tr><tr><td><code>secondary</code></td><td>Slate that keeps the base colour&#39;s hue</td></tr><tr><td><code>success</code></td><td>A deep green, dark enough to carry white</td></tr><tr><td><code>warning</code></td><td>The complementary amber, the one family with dark ink</td></tr><tr><td><code>danger</code></td><td>A muted vermilion</td></tr><tr><td><code>info</code></td><td>The analogous azure</td></tr></tbody></table><h3 id="three-hand-picked-values-per-family" tabindex="-1">Three hand-picked values per family <a class="header-anchor" href="#three-hand-picked-values-per-family" aria-label="Permalink to &quot;Three hand-picked values per family&quot;">​</a></h3><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>--plass-{color}-solid       one end of the sweep, and the family&#39;s identity</span></span>\n<span class="line"><span>--plass-{color}-solid-to    the other end</span></span>\n<span class="line"><span>--plass-{color}-on-solid    the ink on both</span></span>\n<span class="line"><span>--plass-{color}-accent      readable on a surface — per theme</span></span></code></pre></div><p>Everything else (<code>-fill</code>, <code>-tint</code>, <code>-soft</code>, <code>-line</code>, <code>-ring</code>) is computed with <code>color-mix()</code> in the derived block. <strong>Adding a colour family is two edits</strong>, one entry in the <code>PlassColor</code> union and three lines in <code>styles.css</code>, plus its <code>accent</code> in each theme.</p><h3 id="the-key-colour-across-themes" tabindex="-1">The key colour across themes <a class="header-anchor" href="#the-key-colour-across-themes" aria-label="Permalink to &quot;The key colour across themes&quot;">​</a></h3><p><strong>A pane of blue glass is the same pane in a dark room.</strong> What changes in dark mode is the ground it rests on: the clear glass loses its white and becomes a smoked pane, the shadow goes black and deepens, and the tint bleeding into that shadow is turned up, because a coloured shadow has almost nothing to sit on over a near-black page.</p><p>The one family value that <em>is</em> per-theme is <code>accent</code>, the colour that has to be <strong>read</strong> off a surface rather than looked at.</p><h3 id="both-ends-sit-as-close-to-the-contrast-floor-as-the-arithmetic-allows" tabindex="-1">Both ends sit as close to the contrast floor as the arithmetic allows <a class="header-anchor" href="#both-ends-sit-as-close-to-the-contrast-floor-as-the-arithmetic-allows" aria-label="Permalink to &quot;Both ends sit as close to the contrast floor as the arithmetic allows&quot;">​</a></h3><p>Every stop clears 4.5:1 against its own <code>on-solid</code>, and every one of them is within 0.15 of exactly 4.5. That is not timidity in the other direction: a family held further above the floor than it has to be is a family that is <strong>darker than it has to be</strong>, and a set of buttons that are all a shade too deep is the most common way a palette goes quietly wrong.</p><p>So the constraint is two-sided. The floor decides how bright a family may be; nothing else is allowed to make it darker than that.</p><h3 id="warning-has-dark-text" tabindex="-1"><code>warning</code> has dark text <a class="header-anchor" href="#warning-has-dark-text" aria-label="Permalink to &quot;`warning` has dark text&quot;">​</a></h3><p>White on amber does not reach 4.5:1 at any lightness worth calling amber. <code>--plass-warning-on-solid</code> is the one dark brown in the set. Changing the ink is the right answer; distorting the family to preserve a white label is not.</p><hr><h2 id="_3-size-and-density" tabindex="-1">3. Size and density <a class="header-anchor" href="#_3-size-and-density" aria-label="Permalink to &quot;3. Size and density&quot;">​</a></h2><h3 id="size-height-and-type-scale" tabindex="-1"><code>size</code>: height and type scale <a class="header-anchor" href="#size-height-and-type-scale" aria-label="Permalink to &quot;`size`: height and type scale&quot;">​</a></h3><table tabindex="0"><thead><tr><th></th><th>xs</th><th>sm</th><th>md</th><th>lg</th><th>xl</th></tr></thead><tbody><tr><td>Height</td><td>24px</td><td>32px</td><td><strong>40px</strong></td><td>48px</td><td>56px</td></tr><tr><td>Text</td><td>11px</td><td>13px</td><td><strong>14px</strong></td><td>16px</td><td>18px</td></tr><tr><td>Radius</td><td>8px</td><td>10px</td><td><strong>12px</strong></td><td>14px</td><td>16px</td></tr></tbody></table><p>The ladder is a flat 8px per step and it starts higher than a dense desktop toolkit would. That is the material asking for room: a gradient that has to turn, and a hairline, inside 32px is two effects fighting over eleven pixels of fill. <code>xs</code> exists for a table row, and it is the one step where the sweep is short enough that the two ends nearly meet.</p><p><code>lg</code> at 48px and <code>xl</code> at 56px both clear the 44px mobile touch target.</p><h3 id="the-radius-is-a-fillet" tabindex="-1">The radius is a fillet <a class="header-anchor" href="#the-radius-is-a-fillet" aria-label="Permalink to &quot;The radius is a fillet&quot;">​</a></h3><p>It grows far more slowly than the height does, 33% of an <code>xs</code> control, 29% at <code>md</code>, 29% at <code>xl</code>. That near-constant radius is what makes two controls of different sizes read as two pieces cast in the same mould. A radius pinned to a percentage of the height gives you a small pill and a large rectangle instead.</p><h3 id="density-padding-and-only-padding" tabindex="-1"><code>density</code>: padding, and only padding <a class="header-anchor" href="#density-padding-and-only-padding" aria-label="Permalink to &quot;`density`: padding, and only padding&quot;">​</a></h3><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>default   10 / 12 / 16 / 24 / 28px</span></span>\n<span class="line"><span>compact    6 /  8 / 10 / 14 / 16px</span></span></code></pre></div><p><strong>Density touches neither the height nor the type scale.</strong> Two controls of the same <code>size</code> are the same height whatever their density, so a row of mixed-density controls keeps its baseline. The two tracks are roughly 2:1, so the difference is legible at a glance.</p><hr><h2 id="_4-elevation" tabindex="-1">4. Elevation <a class="header-anchor" href="#_4-elevation" aria-label="Permalink to &quot;4. Elevation&quot;">​</a></h2><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">type</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> PlassElevation</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 0</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> |</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 1</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> |</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 2</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> |</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 3</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span></code></pre></div><p><strong>A PlButton defaults to <code>1</code>, and a PlTextField to <code>0</code>.</strong> A key rests <em>on</em> the sheet; a field is cut <em>into</em> it. Hovering adds a level and pressing removes one, so a default button presses down flush against the glass and a raised one comes back to where it was.</p><p>The ladder climbs by <strong>blur far more than by offset</strong>. A surface that moves 20px down the page when it is raised has left the sheet, and everything in this library is still sitting on one.</p><h3 id="shadows-are-tinted-and-this-is-where-plass-parts-company-with-restraint" tabindex="-1">Shadows are tinted, and this is where Plass parts company with restraint <a class="header-anchor" href="#shadows-are-tinted-and-this-is-where-plass-parts-company-with-restraint" aria-label="Permalink to &quot;Shadows are tinted, and this is where Plass parts company with restraint&quot;">​</a></h3><p><code>--plass-{color}-tint</code> is the drop shadow a control casts in its own colour, and it is the single loudest thing in the design language. It is the difference between a button that is blue and a button that is <em>made of</em> blue.</p><p>It is deliberately <strong>not part of the elevation ladder</strong> and does not scale with it. Elevation says how far off the page a surface is; the tint says what the surface is made of. A <code>danger</code> button one level higher is not a redder pane of glass.</p><p>Only <code>solid</code> gets one. A sheet of clear glass casts a neutral shadow, because it has no colour of its own.</p><hr><h2 id="_5-motion" tabindex="-1">5. Motion <a class="header-anchor" href="#_5-motion" aria-label="Permalink to &quot;5. Motion&quot;">​</a></h2><h3 id="controls-do-not-move" tabindex="-1">Controls do not move <a class="header-anchor" href="#controls-do-not-move" aria-label="Permalink to &quot;Controls do not move&quot;">​</a></h3><p><strong>Do not use <code>transform</code> on a control.</strong> Scaling a key resamples its label, and text that shimmers under the cursor undoes the restraint everything else is spending effort on. State changes are expressed in <strong>light and depth</strong> only.</p><p>A surface that <em>holds</em> content rather than being pressed (a Card, a row) may lift, and should. The rule is about the thing under the finger.</p><p>The one position that is animated is a <a href="./../components/inputs/slider"><code>PlSlider</code></a>&#39;s thumb, and it is not an exception: nothing is being shifted off its own place, because the thing being moved <em>is</em> the value. It travels for a step nobody dragged (an arrow key, a press on the rail), and not at all under a finger, where easing towards the pointer would only read as lag.</p><h3 id="drawing-a-mark" tabindex="-1">Drawing a mark <a class="header-anchor" href="#drawing-a-mark" aria-label="Permalink to &quot;Drawing a mark&quot;">​</a></h3><p>The rule above leaves an obvious hole: a tick and a radio dot are not there and then they are, and neither of them may be scaled into place. So they are <strong>drawn</strong> instead.</p><ul><li><strong>A tick draws itself along its own path.</strong> The stroke is dashed at exactly its own length and slid out of view by exactly that, so how much of the mark exists is one number between 0 and 1. In CSS that is a <code>stroke-dashoffset</code>; in Flutter it is the path measured and cut. Nothing appears anywhere it was not going to end up.</li><li><strong>A radio dot grows out of the middle of its ring.</strong> The ring centres a fixed-size child, so a width and a height are laid out about the same point. The box grows, and no <code>transform</code> is involved. Nothing outside the ring can be moved by it.</li></ul><p>The mark is <strong>kept in the document while it is not there</strong>, at zero. A mark that is thrown away the moment the value is cleared cannot travel back out, and a control that animates one way and cuts the other is a control that means two different things by the same click.</p><h3 id="press-is-light-not-paint" tabindex="-1">Press is light, not paint <a class="header-anchor" href="#press-is-light-not-paint" aria-label="Permalink to &quot;Press is light, not paint&quot;">​</a></h3><p>The fill is a gradient, and a gradient cannot be transitioned. So hover and press are <code>filter: brightness()</code>:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>hover   brightness(1.05)  + one level up the elevation ladder, and the tint spreads</span></span>\n<span class="line"><span>active  brightness(0.95)  + one level down, and the tint contracts under it</span></span></code></pre></div><p>Three things move together and they all say the same thing: the control has gone down and there is less room under it for its own shadow.</p><h3 id="one-duration-both-ways" tabindex="-1">One duration, both ways <a class="header-anchor" href="#one-duration-both-ways" aria-label="Permalink to &quot;One duration, both ways&quot;">​</a></h3><p><code>--plass-duration</code> is 150ms and <code>--plass-ease</code> is one curve, applied identically in both directions. A key going down and a key coming back up are the same spring; an asymmetric press belongs to a different material.</p><h3 id="the-second-duration" tabindex="-1">The second duration <a class="header-anchor" href="#the-second-duration" aria-label="Permalink to &quot;The second duration&quot;">​</a></h3><p><code>--plass-duration-slow</code> is 260ms, and exactly one thing decides which of the two a surface gets: <strong>whether it takes the page or hangs off a control.</strong></p><ul><li><strong>Takes the page</strong>: a modal, a drawer, an overlay, a command palette. The sheet and its scrim both fade on the slow duration, so the two arrive as one thing. 150ms across the whole window reads as a cut with a hint of blur on it rather than as a fade, and a page that changes that completely that fast leaves a reader looking for what moved.</li><li><strong>Hangs off a control</strong>: a menu, a popover, a tooltip, a select or combobox list, a date picker&#39;s sheet. These stay at 150ms, because they are the size of the control they came from and a reader is usually on their way to something inside one.</li></ul><p>The same 260ms is also what a height travels over (an accordion, a collapsible, a pill) for the same reason: what is moving is the page under it.</p><h3 id="light-arrives-with-the-pointer" tabindex="-1">Light arrives with the pointer <a class="header-anchor" href="#light-arrives-with-the-pointer" aria-label="Permalink to &quot;Light arrives with the pointer&quot;">​</a></h3><p><code>.plass-glow</code> is two stacked layers on every interactive control.</p><ul><li><strong><code>::before</code> is the bloom</strong>: a soft radial light centred on the pointer, fading in over 240ms when the pointer arrives and following it across the surface.</li><li><strong><code>::after</code> is the press</strong>: the same shape a shade brighter, at <code>0ms</code> in and ~700ms out. The flash lands on the frame of the click and is still visibly draining a beat after the finger lifts.</li></ul><p>Both read <code>--p-mx</code> / <code>--p-my</code>, which the component writes <strong>straight to the element&#39;s inline style</strong> on <code>pointermove</code>.</p><p><strong>Do not hold this in React state.</strong> The event fires at pointer rate, so a <code>setState</code> would re-render the tree on every mouse move. The coordinates come from <code>offsetX</code>/<code>offsetY</code> rather than <code>getBoundingClientRect()</code>, so nothing forces a reflow. Icons carry <code>pointer-events: none</code>, so the offsets are always relative to the control.</p><p>This replaced a static specular highlight, and the reasoning is worth keeping: <strong>a highlight that is always on is a claim about a lamp somewhere off-screen, and it reads as lacquer. Light that arrives with the pointer is a claim about the pointer, which is true.</strong></p><p>The <code>::after</code> layer is also what carries the effect on a touch screen, where there is no hover at all: <code>:active</code> holds for as long as the finger is down and <code>pointermove</code> keeps writing the coordinates, so the light follows a finger dragged across the control.</p><p>The two colour slots switch with the variant, because white light on a near-white sheet is invisible: a filled control gets <code>--plass-glow-on-fill</code> (white at 18%), and a <code>glass</code> or <code>ghost</code> one gets its own family&#39;s soft tint.</p><hr><h2 id="_6-states" tabindex="-1">6. States <a class="header-anchor" href="#_6-states" aria-label="Permalink to &quot;6. States&quot;">​</a></h2><p>The three states each have to speak on their own axis, and each has to be distinguishable from the default at a glance.</p><table tabindex="0"><thead><tr><th>State</th><th>How it is expressed</th><th>Why</th></tr></thead><tbody><tr><td><code>disabled</code></td><td><code>opacity: 0.5</code> and <code>saturate(0.35)</code>; no light, no shadow</td><td>On a page made of translucent sheets, a surface the page shows <em>through</em> has stopped being an object</td></tr><tr><td><code>loading</code></td><td>Unchanged, with a spinner in the <code>startIcon</code> slot</td><td>It is in progress, not unavailable</td></tr><tr><td><code>readOnly</code></td><td>Keeps the colour, goes flat, <code>saturate(0.55)</code></td><td>A label that happens to be control-shaped</td></tr></tbody></table><p>Only <code>disabled</code> uses the native <code>disabled</code> attribute. <code>loading</code> and <code>readOnly</code> are marked with <code>aria-disabled</code>, keep focus, and stop activation in the handler.</p><blockquote><p><strong>Opacity is doing real work here.</strong> The usual complaint against <code>opacity: 0.5</code> is that it reads as &quot;blurry&quot; whatever the state is, and it does, on an opaque page. On this one it reads as the page coming <em>through</em> the control, which is a specific thing to say and the only state that says it. It is the one axis <code>disabled</code> uses and no other state touches.</p></blockquote><hr><h2 id="_7-implementation-rules" tabindex="-1">7. Implementation rules <a class="header-anchor" href="#_7-implementation-rules" aria-label="Permalink to &quot;7. Implementation rules&quot;">​</a></h2><h3 id="branch-state-in-js-not-in-css" tabindex="-1">Branch state in JS, not in CSS <a class="header-anchor" href="#branch-state-in-js-not-in-css" aria-label="Permalink to &quot;Branch state in JS, not in CSS&quot;">​</a></h3><p>Two Tailwind variants of equal specificity are resolved by <strong>their order in the generated stylesheet</strong>. That is not a property a component may depend on.</p><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// like this</span></span>\n<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">disabled </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">?</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> disabledClasses[variant] </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> readOnly </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">?</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> readOnlyClasses[variant] </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> restClasses[variant];</span></span>\n<span class="line"></span>\n<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// not like this — the precedence of data-disabled: against data-readonly: is undefined</span></span>\n<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;data-disabled:opacity-50 data-readonly:saturate-50&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">);</span></span></code></pre></div><h3 id="colour-slots-go-in-inline-styles" tabindex="-1">Colour slots go in inline styles <a class="header-anchor" href="#colour-slots-go-in-inline-styles" aria-label="Permalink to &quot;Colour slots go in inline styles&quot;">​</a></h3><p>Tailwind only ever sees <strong>class names that appear literally in the source</strong>. Hardcoding <code>[--p-fill:var(--plass-primary-fill)]</code> per family means dozens of classes for every colour added. Generate the <code>--p-*</code> slots as inline styles instead.</p><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;--p-fill&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">`var(--plass-${</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">color</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">}-fill)`</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span></code></pre></div><p>This is the only kind of reason to step outside Tailwind: <strong>step outside only when Tailwind cannot express it.</strong></p><h3 id="derived-tokens-are-repeated-per-theme-root" tabindex="-1">Derived tokens are repeated per theme root <a class="header-anchor" href="#derived-tokens-are-repeated-per-theme-root" aria-label="Permalink to &quot;Derived tokens are repeated per theme root&quot;">​</a></h3><p>A custom property resolves its <code>var()</code>s <strong>on the element that declares it</strong>. <code>--plass-primary-tint</code> reads <code>--plass-tint-strength</code>, which is per-theme. Declared only on <code>:root</code> it would freeze to the light theme&#39;s value inside a <code>.dark</code> subtree. That is why the derived block&#39;s selector is <code>:root, .dark, .light, [data-theme=&#39;dark&#39;], [data-theme=&#39;light&#39;]</code>.</p><h3 id="a-portal-s-stacking-level" tabindex="-1">A portal&#39;s stacking level <a class="header-anchor" href="#a-portal-s-stacking-level" aria-label="Permalink to &quot;A portal&#39;s stacking level&quot;">​</a></h3><p>Every surface that leaves the flow, a modal, a drawer, a menu, a select&#39;s list, a popover, a tooltip, a toast. Is painted at <code>var(--plass-z-portal)</code>, which is <code>50</code> and is one line for a page to change. Which thing floats over which is a decision an app has already made by the time it reaches for a dialog: it has a header, or a cookie bar, or a video player, and a number chosen here is a guess at somebody else&#39;s ladder.</p><p>They all read the <strong>same</strong> token on purpose. Moving them apart is how a select opened inside a modal ends up behind it.</p><p>It is declared on a bare <code>:root</code> and repeated in neither theme block, which is the exception to the rule above and for the same reason it exists: a theme block re-declares what it carries, so a token living in one would reset a consumer&#39;s own value on every nested element wearing a theme class.</p><h3 id="move-it-to-css-when-it-stops-being-readable" tabindex="-1">Move it to CSS when it stops being readable <a class="header-anchor" href="#move-it-to-css-when-it-stops-being-readable" aria-label="Permalink to &quot;Move it to CSS when it stops being readable&quot;">​</a></h3><p><code>.plass-glow</code> is a real class in <code>styles.css</code> rather than a set of Tailwind utilities because <code>[&amp;::before]:[background:radial-gradient(…)]</code> is technically expressible and impossible to maintain. Styling that puts a gradient on a pseudo-element belongs in CSS.</p><h3 id="never-use-outline-none" tabindex="-1">Never use <code>outline-none</code> <a class="header-anchor" href="#never-use-outline-none" aria-label="Permalink to &quot;Never use `outline-none`&quot;">​</a></h3><p>Tailwind v4&#39;s <code>outline-*</code> utilities route the style through <code>--tw-outline-style</code>. An <code>outline-none</code> anywhere on the element sets that variable to <code>none</code> and <strong>the focus ring disappears entirely.</strong> Use the shorthand.</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>focus-visible:[outline:2px_solid_var(--p-ring)] focus-visible:[outline-offset:0px]</span></span></code></pre></div><h3 id="ring-placement" tabindex="-1">Ring placement <a class="header-anchor" href="#ring-placement" aria-label="Permalink to &quot;Ring placement&quot;">​</a></h3><p>The offset is <code>0</code>, and it is <code>0</code> on every control in the library. A ring held 2px off a control that draws an edge of its own (a field, a select, a tick, a switch) is read as three concentric rectangles round one object, and the object looks as though it has come loose from the ring. Flush, the outline sits directly against the outside of the edge, and the edge simply thickens and takes the family&#39;s colour.</p><p>Nothing is lost on a control with no edge either: an outline is always drawn <strong>outside</strong> the border box, so on a filled key it is a rim against the page rather than a band over the fill.</p><p>The one exception is a control that something else clips, a tab on a rail, a segment in a groove, a row inside a rounded sheet, an accordion header in a scored pane. Those take <code>focus-visible:[outline-offset:-2px]</code>, because a ring drawn outside them is a ring with its top or its bottom sliced off.</p><h3 id="the-ring-is-an-outline" tabindex="-1">The ring is an <code>outline</code> <a class="header-anchor" href="#the-ring-is-an-outline" aria-label="Permalink to &quot;The ring is an `outline`&quot;">​</a></h3><p>Tailwind&#39;s <code>ring-*</code> is a <code>box-shadow</code>, and every Plass surface already spends its <code>box-shadow</code> on the elevation, the tint and the glass hairline. A ring would have to be spliced into that chain in each of the three variants, and the first one that forgot would silently lose its focus ring.</p>',123)])])}const u=t(i,[["render",n]]);export{g as __pageData,u as default};
