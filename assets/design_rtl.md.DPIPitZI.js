import{_ as s,C as o,o as n,c as d,j as t,a,E as r,a2 as l}from"./chunks/framework.M7K9TQZf.js";const w=JSON.parse('{"title":"Right to left","description":"One attribute on one element. Every component is laid out in logical properties, so dir=\\"rtl\\" is the whole of it. There is nothing to configure and no…","frontmatter":{"title":"Right to left","order":5},"headers":[],"relativePath":"design/rtl.md","filePath":"en/design/rtl.md","lastUpdated":1788614679000}'),h={name:"design/rtl.md"};function c(p,e,g,u,k,m){const i=o("Demo");return n(),d("div",null,[e[0]||(e[0]=t("h1",{id:"right-to-left",tabindex:"-1"},[a("Right to left "),t("a",{class:"header-anchor",href:"#right-to-left","aria-label":'Permalink to "Right to left"'},"​")],-1)),e[1]||(e[1]=t("p",{class:"plass-lede"},[a("One attribute on one element. Every component is laid out in logical properties, so "),t("code",null,'dir="rtl"'),a(" is the whole of it. There is nothing to configure and no direction to declare twice.")],-1)),r(i,{src:"rtl/direction","min-height":420}),e[2]||(e[2]=l(`<div class="plass-fw" data-fw="react"><div class="language-html vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">html</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&lt;</span><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">html</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> dir</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">=</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;rtl&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&gt;&lt;/</span><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">html</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&gt;</span></span></code></pre></div><p>That is the layout, and it needs no JavaScript at all. <strong>A handful of behaviours read the direction in JavaScript</strong> (a slider&#39;s arrow keys, the way ←/→ walk a tab list, which physical edge a popup&#39;s <code>align=&quot;start&quot;</code> resolves to), and those hear about it through a <a href="./../guide/defaults"><code>PlassProvider</code></a>, which reads the document&#39;s own direction. Nothing to pass; the provider is the wire, not a second place to declare the answer.</p><div class="language-tsx vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">tsx</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&lt;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">PlassProvider</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&gt;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  &lt;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">App</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> /&gt;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&lt;/</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">PlassProvider</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&gt;</span></span></code></pre></div></div><div class="plass-fw" data-fw="flutter"><p>Flutter reads <code>Directionality</code> from the widget tree, and <code>MaterialApp</code>/<code>WidgetsApp</code> set it from the locale. Nothing in this package needs telling either:</p><div class="language-dart vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">dart</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">Directionality</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(textDirection</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> TextDirection</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">.rtl, child</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> child);</span></span></code></pre></div></div><h2 id="the-rule" tabindex="-1">The rule <a class="header-anchor" href="#the-rule" aria-label="Permalink to &quot;The rule&quot;">​</a></h2><p><strong><code>start</code>/<code>end</code>, never <code>left</code>/<code>right</code>.</strong> Every padding, margin, border, radius and inset in the library is written as a logical property, so what a component calls its start edge is the left one in English and the right one in Arabic.</p><div class="plass-fw" data-fw="react"><p>Nothing measures the direction to decide; the browser does it.</p></div><div class="plass-fw" data-fw="flutter"><p>Nothing measures the direction to decide; <code>EdgeInsetsDirectional</code>, <code>PositionedDirectional</code>, <code>AlignmentDirectional</code>, <code>BorderRadiusDirectional</code> and <code>BorderDirectional</code> resolve themselves against the ambient <code>Directionality</code>. A <code>Row</code>, a <code>CrossAxisAlignment.start</code> and a <code>TextAlign.start</code> already do.</p></div><p>The same rule reaches the prop vocabulary: <a href="./prop-conventions"><code>PlassAlign</code></a> is <code>start | center | end</code> for exactly this reason, and a <code>PlSidebar</code> takes a <code>start</code> side rather than a left one.</p><h2 id="flipped-and-unflipped" tabindex="-1">Flipped and unflipped <a class="header-anchor" href="#flipped-and-unflipped" aria-label="Permalink to &quot;Flipped and unflipped&quot;">​</a></h2><table tabindex="0"><thead><tr><th></th><th></th></tr></thead><tbody><tr><td>Padding, margins, borders, radii, insets</td><td><strong>Flip.</strong> They are logical properties</td></tr><tr><td>Text alignment, list markers, table columns</td><td><strong>Flip.</strong> The browser&#39;s own</td></tr><tr><td>A chevron that points along the reading direction, a breadcrumb&#39;s, a pagination stepper&#39;s, a submenu&#39;s, a <code>PlTree</code>&#39;s closed twisty</td><td><strong>Flip.</strong> One glyph, turned</td></tr><tr><td>A <code>PlSwitch</code>&#39;s thumb</td><td><strong>Flips.</strong> Off is the inline start, which is the right-hand end under RTL, as every platform&#39;s own switch behaves</td></tr><tr><td>A <code>PlPanes</code> handle, a <code>PlSidebar</code> drag, a <code>PlCarousel</code> or <code>PlScrollZone</code> strip</td><td><strong>Flip</strong>, including the arrow keys</td></tr><tr><td>A <code>PlSlider</code>&#39;s run</td><td><strong>Flips.</strong> The minimum is at the inline start, so the paint, the press mapping and the left/right arrow keys turn over together</td></tr><tr><td>A <code>PlChatBubble</code>&#39;s tail corner, a <code>PlButtonGroup</code>&#39;s squared edges, a date range&#39;s open and closed ends</td><td><strong>Flip.</strong> They face the reader&#39;s start</td></tr><tr><td>A <code>PlAnimateMarquee</code></td><td><strong>Flips.</strong> A strip travels towards the reading start, so the words arrive in the order they are read</td></tr><tr><td><code>PlassSide</code>, a tooltip&#39;s <code>side</code>, a drawer&#39;s edge</td><td><strong>Physical, on purpose.</strong> A tooltip above a button is above it in every writing direction</td></tr><tr><td>A <code>PlColorPicker</code>&#39;s rails</td><td><strong>Do not flip.</strong> A hue rail is a colour space rather than a reading axis: 0° sits where 0° sits in every picker, and a mirrored one would be unrecognisable</td></tr><tr><td>A <code>PlSkeleton</code>&#39;s sweep</td><td><strong>Does not flip.</strong> It is a light crossing a surface, and a light that changed direction with the locale would read as a different material</td></tr><tr><td>An icon that is not directional, a star, a bin, a spinner</td><td>Does not flip, and should not</td></tr><tr><td>Numbers, dates and times</td><td>The platform&#39;s own. Set <code>locale</code> on the components that take one</td></tr></tbody></table><h2 id="reading-the-direction-in-code" tabindex="-1">Reading the direction in code <a class="header-anchor" href="#reading-the-direction-in-code" aria-label="Permalink to &quot;Reading the direction in code&quot;">​</a></h2><p>Almost nothing needs to. The exceptions are the places where the <strong>thing being measured</strong> is physical too, and pairing a logical property with a physical measurement is what would actually break the direction.</p><div class="plass-fw" data-fw="react"><p>Three, and all three read <code>getComputedStyle(…).direction</code> rather than guessing:</p><ul><li>a <strong><code>PlPanes</code></strong> handle dragged with the pointer or nudged with the arrow keys. A pointer&#39;s <code>clientX</code> grows to the right in both directions, so the delta has to be turned round;</li><li>a <strong><code>PlSidebar</code></strong>&#39;s resize drag, for the same reason, and the edge a collapsed one turns into a <code>PlDrawer</code> on;</li><li>the moving indicator in <strong><code>PlTabs</code></strong>, <strong><code>PlSegmentedButton</code></strong> and <strong><code>PlFloatingBottomNavigation</code></strong>, which is placed from <code>offsetLeft</code>, a distance from the left edge in both directions.</li></ul><p>Base UI&#39;s own primitives read it from a <strong>React context</strong> instead, and that is the one thing a page has to do something about: with no provider its <code>useDirection()</code> answers <code>ltr</code> however the document is written. <code>PlassProvider</code> renders that context from the document&#39;s direction, which is why a page that set <code>dir</code> and nothing else would look right and behave the other way round.</p><p>There is one place CSS answers it instead of JavaScript, and it is the exception that proves the rule: there is no logical <code>translate</code>, so <code>.plass-marquee-track</code> flips its sign under <code>[dir=&#39;rtl&#39;]</code>.</p></div><div class="plass-fw" data-fw="flutter"><p>All of them read <code>Directionality.of(context)</code>, and they fall into three kinds:</p><ul><li><strong>A pointer or an arrow key against a physical axis.</strong> A drag&#39;s <code>delta.dx</code> grows to the right in both directions, so <code>PlPanes</code>, <code>PlSidebar</code>, <code>PlSlider</code> and <code>PlScrollZone</code> turn it round, and with it the left/right arrow keys, which mean &quot;further along the line&quot; rather than &quot;further right&quot;.</li><li><strong>A corner that has to be handed over resolved.</strong> <code>PlButtonGroup</code>&#39;s squared edges, <code>PlChatBubble</code>&#39;s tail and a date range&#39;s open and closed ends are written as a <code>BorderRadius</code> rather than as a <code>BorderRadiusDirectional</code>, because the same value reaches a <code>ClipRRect</code>, a <code>BoxDecoration</code> and a painter, and the painter takes a resolved one.</li><li><strong>A <code>PlassSide</code> chosen for the reader.</strong> <code>PlassSide</code> names an edge of the screen, so a <code>PlNavigationMenu</code> picks which edge its panel flies out towards rather than always taking the right.</li></ul><p>A <strong><code>PlSlider</code></strong> is worth naming, because it turns over more than a margin: the paint, the press mapping and the left/right arrow keys mirror together, and a control where only some of them did would be arguing with itself.</p><p>Everything else is a <code>*Directional</code> widget, and the package test below is what keeps it that way.</p></div><h2 id="checking-your-own" tabindex="-1">Checking your own <a class="header-anchor" href="#checking-your-own" aria-label="Permalink to &quot;Checking your own&quot;">​</a></h2><div class="plass-fw" data-fw="react"><div class="language-tsx vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">tsx</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&lt;</span><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">div</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> dir</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;rtl&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&gt;{</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">/* a screen */</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}&lt;/</span><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">div</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&gt;</span></span></code></pre></div><p><code>dir</code> can go on any element, so a single component can be checked without the whole page moving. A subtree that runs the other way from its page wants a <code>PlassProvider direction=&quot;rtl&quot;</code> around it as well, for the same reason the page does.</p></div><div class="plass-fw" data-fw="flutter"><div class="language-dart vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">dart</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">Directionality</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(textDirection</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> TextDirection</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">.rtl, child</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> screen);</span></span></code></pre></div><p><code>Directionality</code> can wrap any subtree, so a single widget can be checked without the whole app moving.</p></div><p>What to look for is a gap that has landed on the wrong side, an icon that should have turned and did not, and text that is still ragged on the wrong edge.</p><p>A component that gets it wrong is a bug. Both packages carry a test for it, in two halves: one drives a real right-to-left tree and asserts the handful of behaviours that must turn over, and the other reads every component&#39;s source and fails on a physical property that is not on a short, documented list. The second half is the one that catches the <em>next</em> component.</p><h2 id="notes" tabindex="-1">Notes <a class="header-anchor" href="#notes" aria-label="Permalink to &quot;Notes&quot;">​</a></h2><ul><li>The library ships no translations. <code>PlTable</code>&#39;s <code>empty</code>, <code>PlPagination</code>&#39;s labels, <code>PlAlert</code>&#39;s <code>closeLabel</code> and the pickers&#39; <code>labels</code> are plain props, and the app-wide defaults set the picker vocabulary once. A library that shipped translations would have to be told which language a page is in, and the page already knows.</li><li>The <code>locale</code> default reaches the date, time and number components. It does not set the direction, that belongs to the document in React and to the app in Flutter, and a component library has no business writing on either.</li></ul>`,20))])}const b=s(h,[["render",c]]);export{w as __pageData,b as default};
