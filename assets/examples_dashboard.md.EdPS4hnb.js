import{_ as a,C as r,o as c,c as n,j as t,a as o,E as l,a2 as s}from"./chunks/framework.M7K9TQZf.js";const f=JSON.parse('{"title":"Admin dashboard","description":"The back office of Grange, a shop that does not exist. A rail, an app bar, four figures, a filter row and a table with an action on every row, all on one…","frontmatter":{"title":"Admin dashboard","order":1,"aside":false},"headers":[],"relativePath":"examples/dashboard.md","filePath":"en/examples/dashboard.md","lastUpdated":1788614679000}'),i={name:"examples/dashboard.md"};function h(p,e,m,u,P,b){const d=r("Demo");return c(),n("div",null,[e[0]||(e[0]=t("h1",{id:"admin-dashboard",tabindex:"-1"},[o("Admin dashboard "),t("a",{class:"header-anchor",href:"#admin-dashboard","aria-label":'Permalink to "Admin dashboard"'},"​")],-1)),e[1]||(e[1]=t("p",{class:"plass-lede"},[o("The back office of Grange, a shop that does not exist. A rail, an app bar, four figures, a filter row and a table with an action on every row, all on one screen and all at the same "),t("code",null,"size"),o(", which is the arrangement that shows whether a size ladder actually holds.")],-1)),l(d,{src:"examples/dashboard",flutter:!1,"min-height":900}),e[2]||(e[2]=s('<p>The whole screen is one file: <code>docs/.vitepress/demos/examples/dashboard.tsx</code>. It is live, search the table, filter it by channel or status, tick some rows and the bulk actions appear.</p><h2 id="composition" tabindex="-1">Composition <a class="header-anchor" href="#composition" aria-label="Permalink to &quot;Composition&quot;">​</a></h2><table tabindex="0"><thead><tr><th>Block</th><th>Components</th><th>Worth noticing</th></tr></thead><tbody><tr><td>Rail</td><td><code>PlList</code> <code>PlListItem</code> <code>PlIcon</code> <code>PlBadge</code> <code>PlPill</code> <code>PlCard</code> <code>PlProgressLinear</code></td><td><code>render={&lt;nav /&gt;}</code> makes the list a real landmark, <code>selected</code> marks the current row, and the unread count sits in <code>endIcon</code></td></tr><tr><td>App bar</td><td><code>PlToolbar</code> <code>PlBreadcrumb</code> <code>PlBadge</code> <code>PlIconButton</code> <code>PlTooltip</code> <code>PlAvatar</code></td><td><code>position=&quot;sticky&quot;</code> keeps the actions reachable while the page scrolls under them</td></tr><tr><td>Alert</td><td><code>PlAlert</code></td><td>One thing needs attention, said once, at the top, with its own <code>action</code></td></tr><tr><td>Figures</td><td><code>PlCard</code> <code>PlTypography</code> <code>PlProgressLinear</code> <code>PlProgressCircular</code></td><td>A bar for progress towards a target, a ring where the figure <em>is</em> a share of a whole</td></tr><tr><td>Filters</td><td><code>PlTextField</code> <code>PlSelect</code> <code>PlDateRangePicker</code> <code>PlSegmentedButton</code></td><td>At <code>size=&quot;sm&quot;</code> the field, the select and the range picker are the same height, so the row keeps one baseline</td></tr><tr><td>Table</td><td><code>PlTable</code> <code>PlCheckbox</code> <code>PlChip</code> <code>PlMenu</code> <code>PlIconButton</code> <code>PlPagination</code></td><td>Select-all is an <code>indeterminate</code> checkbox in the header cell, and every row carries its own <code>PlMenu</code></td></tr><tr><td>Bulk actions</td><td><code>PlButton</code> <code>PlModal</code> <code>PlToast</code></td><td>They appear only with a selection; the destructive one confirms in a <code>PlModal</code> and reports with an undoable toast</td></tr><tr><td>Bottom row</td><td><code>PlCard</code> <code>PlTimeline</code> <code>PlSwitch</code> <code>PlDivider</code></td><td>What happened, and what reaches you, the same card at the same elevation, twice</td></tr><tr><td>Settings</td><td><code>PlDrawer</code> <code>PlSelect</code> <code>PlSwitch</code></td><td>The settings that do not belong on the screen, in a sheet that slides over it</td></tr></tbody></table><h2 id="notes" tabindex="-1">Notes <a class="header-anchor" href="#notes" aria-label="Permalink to &quot;Notes&quot;">​</a></h2><ul><li>The status filter is a <code>PlSegmentedButton</code> rather than <code>PlTabs</code>. Nothing below it is a panel, it is the same table either way, and a segmented button is one value out of four.</li><li>Filtering is ordinary React state. <code>PlTable</code> renders whatever it is handed and shows <code>empty</code> when that is nothing.</li><li>Every row action has an accessible name that says which row it belongs to, because <code>label</code> on the trigger <code>PlIconButton</code> includes the order id.</li><li>The bulk-action <code>PlModal</code> passes <code>modal=&quot;trap-focus&quot;</code>. A fully modal dialog makes the page behind it inert, which is right in an app and wrong inside a documentation preview.</li></ul><h2 id="next" tabindex="-1">Next <a class="header-anchor" href="#next" aria-label="Permalink to &quot;Next&quot;">​</a></h2><ul><li>Two more whole screens: <a href="./landing">Landing page</a> and <a href="./signup">Sign-up</a>.</li><li>Per-component props and examples are under <a href="./../components/">Components</a>.</li></ul>',7))])}const w=a(i,[["render",h]]);export{f as __pageData,w as default};
