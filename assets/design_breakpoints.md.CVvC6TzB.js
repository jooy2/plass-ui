import{_ as t,C as i,o as n,c as o,a2 as a,j as d,a as r,E as h}from"./chunks/framework.M7K9TQZf.js";const y=JSON.parse('{"title":"Breakpoints","description":"Five names, four widths, and one place they are written down. Everything in the library that changes with the width of the window reads the same ladder,…","frontmatter":{"title":"Breakpoints","order":4},"headers":[],"relativePath":"design/breakpoints.md","filePath":"en/design/breakpoints.md","lastUpdated":1788614679000}'),l={name:"design/breakpoints.md"};function p(c,e,k,g,u,w){const s=i("Fw");return n(),o("div",null,[e[1]||(e[1]=a(`<h1 id="breakpoints" tabindex="-1">Breakpoints <a class="header-anchor" href="#breakpoints" aria-label="Permalink to &quot;Breakpoints&quot;">​</a></h1><p class="plass-lede">Five names, four widths, and one place they are written down. Everything in the library that changes with the width of the window reads the same ladder, including your <code>md:</code> utilities, which is the point.</p><h2 id="the-ladder" tabindex="-1">The ladder <a class="header-anchor" href="#the-ladder" aria-label="Permalink to &quot;The ladder&quot;">​</a></h2><table tabindex="0"><thead><tr><th>Rung</th><th>From</th><th>Notes</th></tr></thead><tbody><tr><td><code>xs</code></td><td>0</td><td>Everything is at or above it. It has no floor, which is why it is never a bound.</td></tr><tr><td><code>sm</code></td><td>40rem · 640px</td><td></td></tr><tr><td><code>md</code></td><td>48rem · 768px</td><td></td></tr><tr><td><code>lg</code></td><td>64rem · 1024px</td><td></td></tr><tr><td><code>xl</code></td><td>80rem · 1280px</td><td></td></tr></tbody></table><p>They are the same five names as <code>size</code>, deliberately. A reader who has learned the ladder once should not have to learn a second set of words for where a screen changes shape. They are not the same ladder, a size is how tall a control is and a breakpoint is how wide the window is, but they run in the same direction and turn up in the same sentence.</p><div class="plass-fw" data-fw="react"><p>The widths are <strong>Tailwind&#39;s own</strong>, so a layout decided by a <code>md:</code> utility and a layout decided by this library change at the same moment. A page with two answers about how wide it is is a page that drifts by a few pixels for no reason anybody can find later.</p></div><h2 id="moving-them" tabindex="-1">Moving them <a class="header-anchor" href="#moving-them" aria-label="Permalink to &quot;Moving them&quot;">​</a></h2><div class="plass-fw" data-fw="react"><p>One line, in your own Tailwind theme:</p><div class="language-css vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">css</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">@import</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;tailwindcss&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">@import</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;plass-ui/tailwind.css&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">@theme</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  --breakpoint-md: 50rem;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><p>Both halves of the library follow it, <code>PlGrid</code>&#39;s cascade, <code>PlContainer</code>&#39;s measure, <code>PlShow</code>, <code>usePlBreakpoint</code>, and a <code>PlSidebar</code>&#39;s <code>collapseBelow</code>.</p><p><strong>There is no provider prop for this, and there cannot be.</strong> A media query&#39;s <em>condition</em> cannot read a custom property: <code>@media (width &gt;= var(--x))</code> is not valid CSS and never will be. So a breakpoint the stylesheet decides at is resolved when the stylesheet is compiled, not when a component renders, the library writes its own with <code>@variant</code>, which is Tailwind&#39;s, which is your theme&#39;s.</p><p>The JavaScript half is the other kind of question. There a breakpoint is a <em>value</em> rather than a condition, so it can be read off the document, and the stylesheet publishes it as four tokens for exactly that:</p><div class="language-css vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">css</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">--plass-breakpoint-sm: var(--breakpoint-sm, 40rem);</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">--plass-breakpoint-md: var(--breakpoint-md, 48rem);</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">--plass-breakpoint-lg: var(--breakpoint-lg, 64rem);</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">--plass-breakpoint-xl: var(--breakpoint-xl, 80rem);</span></span></code></pre></div><p>Set the Tailwind variable, not these. Setting one of these moves the JavaScript half on its own, which leaves the two disagreeing. The exact failure the arrangement exists to prevent.</p><p><strong>On the precompiled path this is baked.</strong> A project that imports <code>plass-ui/styles.css</code> gets a stylesheet we compiled, with our widths in it; there is no Tailwind on that side to re-run. Import <code>plass-ui/tailwind.css</code> instead if you need to move a breakpoint.</p></div><div class="plass-fw" data-fw="flutter"><p>The ladder is <code>PlassBreakpoint</code>, and its widths are the React package&#39;s, which are Tailwind&#39;s. There is no stylesheet here and so nothing to compile against: the widths are fixed.</p></div><h2 id="responsive-values" tabindex="-1">Responsive values <a class="header-anchor" href="#responsive-values" aria-label="Permalink to &quot;Responsive values&quot;">​</a></h2>`,10)),d("p",null,[e[0]||(e[0]=r("A value that changes with the width is written the same way everywhere in the library. ",-1)),h(s,{react:"A bare value applies at every width; a map applies each entry from its own breakpoint up.",flutter:"The base value applies from zero up and every override applies from its own breakpoint up."})]),e[2]||(e[2]=a(`<div class="plass-fw" data-fw="react"><div class="language-tsx vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">tsx</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&lt;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">PlGridItem</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> span</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">6</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">} /&gt;                    </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// six columns at every width</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&lt;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">PlGridItem</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> span</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{{ xs: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">12</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, md: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">6</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> }} /&gt;    </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// full on a phone, half from 48rem</span></span></code></pre></div><p>There is no <code>xs</code> fallback to write out: an entry cascades to the widths above it, which is what keeps a responsive prop to the breakpoints it actually names. <code>{ lg: 3 }</code> names one rung, not five.</p></div><div class="plass-fw" data-fw="flutter"><div class="language-dart vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">dart</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">PlGridItem</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(span</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> const</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> PlassResponsive</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&lt;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">int</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&gt;(</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">6</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">));               </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// six everywhere</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">PlGridItem</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(span</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> const</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> PlassResponsive</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&lt;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">int</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&gt;(</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">12</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, md</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 6</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">));       </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// full, then half</span></span></code></pre></div><p>Dart has no untagged union, so the base value is the first positional argument and the overrides are named. <code>PlassResponsive(6)</code> is the whole of &quot;six columns everywhere&quot;.</p></div><p>Which props take one:</p><table tabindex="0"><thead><tr><th>Component</th><th>Prop</th></tr></thead><tbody><tr><td><a href="./../components/layout/grid"><code>PlGrid</code></a></td><td><code>columns</code> <code>spacing</code> <code>rowSpacing</code> <code>columnSpacing</code></td></tr><tr><td><a href="./../components/layout/grid"><code>PlGridItem</code></a></td><td><code>span</code> <code>offset</code></td></tr><tr><td><a href="./../components/layout/container"><code>PlContainer</code></a> <a href="./../components/layout/header"><code>PlHeader</code></a> <a href="./../components/layout/footer"><code>PlFooter</code></a></td><td><code>maxWidth</code></td></tr><tr><td><a href="./../components/layout/panes"><code>PlPanes</code></a> <a href="./../components/surfaces/tabs"><code>PlTabs</code></a> <a href="./../components/layout/scroll-zone"><code>PlScrollZone</code></a> <a href="./../components/display/timeline"><code>PlTimeline</code></a> <a href="./../components/navigation/stepper"><code>PlStepper</code></a></td><td><code>orientation</code></td></tr><tr><td><a href="./../components/layout/stack"><code>PlStack</code></a></td><td><code>direction</code></td></tr></tbody></table><h2 id="resolving-a-responsive-value" tabindex="-1">Resolving a responsive value <a class="header-anchor" href="#resolving-a-responsive-value" aria-label="Permalink to &quot;Resolving a responsive value&quot;">​</a></h2><div class="plass-fw" data-fw="react"><p>This is the part worth understanding, because it decides what a responsive prop costs and what it can do.</p><p><strong>A value that decides only style is resolved in CSS.</strong> The component writes one <code>--p-*</code> custom property per rung the caller named and the stylesheet cascades it down from the rung above. Nothing measures anything, nothing re-renders when a window is dragged, and the first paint a server sends is already correct at every width, because the browser is what resolves it. <code>PlGrid</code> and <code>PlContainer</code> work this way, and <code>PlShow</code> is the same idea with <code>display</code>.</p><p><strong>A value that decides structure cannot be.</strong> An orientation changes which DOM a component builds, which ARIA it claims and which way its arrow keys go, and no stylesheet can do that. Those are resolved by <a href="./../hooks/use-breakpoint"><code>usePlBreakpointValue</code></a>, which is JavaScript, so a server renders the <code>xs</code> entry and the browser corrects it on hydration.</p><p>The library reaches for the CSS half whenever it can, and you should too.</p></div><div class="plass-fw" data-fw="flutter"><p>Everything is resolved in the widget, against <code>MediaQuery.sizeOf(context).width</code>, and it is correct on the first frame. There is no server render to disagree with, so the split the React build has to make does not arise here. The breakpoint is the <strong>window&#39;s</strong> width rather than the widget&#39;s own box, which is what a media query measures and what makes two widgets side by side agree about which rung they are on however wide each of them ended up.</p></div><h2 id="showing-one-thing-or-another" tabindex="-1">Showing one thing or another <a class="header-anchor" href="#showing-one-thing-or-another" aria-label="Permalink to &quot;Showing one thing or another&quot;">​</a></h2><p><a href="./../components/layout/show"><code>PlShow</code></a> is the gate: <code>from</code>, <code>until</code>, or both as a band.</p><div class="plass-fw" data-fw="react"><p>It hides with <code>display: none</code>, so both halves are in the document and neither is read out twice. When only one of them should exist at all (it fetches, it is expensive, it holds state) use <a href="./../hooks/use-breakpoint"><code>usePlBreakpointValue</code></a>, and mount one.</p></div><h2 id="asking-in-javascript" tabindex="-1">Asking in JavaScript <a class="header-anchor" href="#asking-in-javascript" aria-label="Permalink to &quot;Asking in JavaScript&quot;">​</a></h2><div class="plass-fw" data-fw="react"><p>Three hooks, for the decisions CSS cannot make, how many items to fetch, which of two components to mount, how many characters to truncate at.</p><ul><li><a href="./../hooks/use-breakpoint"><code>usePlBreakpoint()</code></a>: which rung the window is on.</li><li><a href="./../hooks/use-breakpoint"><code>usePlBreakpointValue(value)</code></a>: a responsive value, resolved. The same shape and the same rule as a responsive prop.</li><li><a href="./../hooks/use-media-query"><code>usePlMediaQuery(query)</code></a>: any query at all.</li></ul><p>All three answer <strong><code>false</code> / <code>xs</code> on a server</strong>, and that is not a bug to work around: it is what makes the markup a server sends deterministic. Anything that has to be right in the first frame belongs in CSS, a Tailwind variant, or <code>PlShow</code>. These are for what comes after.</p></div><div class="plass-fw" data-fw="flutter"><p><code>PlassBreakpoint.of(MediaQuery.sizeOf(context).width)</code> is the whole of it, and <code>PlassResponsive.resolve(breakpoint)</code> reads a responsive value against it.</p></div><h2 id="plsidebar-s-own-breakpoint" tabindex="-1">PlSidebar&#39;s own breakpoint <a class="header-anchor" href="#plsidebar-s-own-breakpoint" aria-label="Permalink to &quot;PlSidebar&#39;s own breakpoint&quot;">​</a></h2><p><a href="./../components/layout/sidebar"><code>PlSidebar</code></a> collapses into a drawer below <code>collapseBelow</code>, and it is worth reading as the worked example of everything above: the decision is a media query in CSS for the first paint <strong>and</strong> <code>matchMedia</code> in JavaScript from then on, because the markup a server sends is the column and a phone must not draw one and throw it away.</p>`,15))])}const v=t(l,[["render",p]]);export{y as __pageData,v as default};
